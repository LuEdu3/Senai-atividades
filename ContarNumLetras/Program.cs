/*Console.Write("Escreva uma palavra: ");



string palavra = Console.ReadLine();

Console.Write("Insira a letra que quer contar: ");

char letra = char.Parse(Console.ReadLine());

int total = 0;

for(int contabil = 0; contabil < palavra.Length; contabil++)
{
    if(letra == palavra[contabil])
    {
        total++;
    }
}
Console.Write($"A letra {letra} foi contada {total} vezes.");


Console.Write("Escreva uma palavra: ");

string palavra = Console.ReadLine();

Console.Write("Escreva a letra a contar: ");

char letra = char.Parse(Console.ReadLine());

int total = 0;

for(int i = 0; i <palavra.Length; i++)
{ 
    if(letra == palavra[i])
    {
        total++;
    }
}
Console.Write($"A letra {letra} repetiu {total} vezes.");



Console.Write("Insira uma palavra: ");

string palavra = Console.ReadLine();

Console.Write("Insira a letra a ser contada: ");

char letra = char.Parse(Console.ReadLine());

int total = 0;

for(int i = 0; i < palavra.Length; i++)
{
    if(letra == palavra[i])
    {
        total++;
    }
}
Console.Write($"A letra {letra} foi contada {total} vezes.");
*/

Console.Write("Insira uma palavra: ");
string palavra = Console.ReadLine();

Console.Write("Insira a letra a ser contada: ");

char letra = char.Parse(Console.ReadLine());

int total = 0;

for(int i = 0;i <palavra.Length;i++)
{
    if(letra == palavra[i])
    {
        total++;
    }
}
Console.Write($"A letra {letra} foi contada {total} vezes.");